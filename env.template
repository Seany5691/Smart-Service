# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/smart_ticketing?schema=public"

# NextAuth Configuration
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# WhatsApp Business API (to be configured later)
WHATSAPP_API_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_WEBHOOK_VERIFY_TOKEN=""

# File Upload Settings
MAX_FILE_SIZE_MB=10
UPLOAD_DIR="./public/uploads"

# Instructions:
# 1. Copy this file to .env.local
# 2. Update DATABASE_URL with your PostgreSQL credentials
# 3. Generate a random NEXTAUTH_SECRET (use: openssl rand -base64 32)
# 4. Configure WhatsApp API credentials when ready
